;(function($, document, window, undefined) {
  'use strict';

  $('.game-container').live('click', '.column', function() {
    // console.log(this.find('.tile'));

    // this.find('.tile').get(0).toggleClass('active');
  });
})($, document, window);